{
  "name": "@db-x-examples/sqlite",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "DB-X demo: a todos schema applied to a local SQLite file (dbx.tsx).",
  "scripts": {
    "preview": "db-x preview ./dbx.tsx",
    "apply": "db-x apply ./dbx.tsx",
    "restore": "db-x restore",
    "destroy": "db-x destroy ./dbx.tsx"
  },
  "dependencies": {
    "@db-x/sqlite-library": "workspace:*",
    "@db-x/runtime": "workspace:*",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@db-x/cli": "workspace:*"
  }
}
