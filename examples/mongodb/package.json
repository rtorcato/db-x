{
  "name": "@db-x-examples/mongodb",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "DB-X demo: a todos collection, indexes and JSON Schema validator applied to MongoDB via <Mongo> — the document-store model, not <Table>/<Column>.",
  "scripts": {
    "preview": "db-x preview ./dbx.tsx",
    "apply": "db-x apply ./dbx.tsx",
    "restore": "db-x restore",
    "destroy": "db-x destroy ./dbx.tsx"
  },
  "dependencies": {
    "@db-x/mongodb-library": "workspace:*",
    "@db-x/runtime": "workspace:*",
    "@rtorcato/js-common": "^2.0.1",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@db-x/cli": "workspace:*"
  }
}
