# SQLite example — placeholder

Not built yet. This directory reserves the path for the SQLite example.

- **Example issue:** #34
- **Blocked by:** #28 (`@db-x/sqlite-library`)

Once the library lands, this will mirror [`examples/postgres/`](../postgres/) —
a `dbx.tsx` schema + `package.json` + `.env.example` — targeting SQLite.
