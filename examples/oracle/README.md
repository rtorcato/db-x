# Oracle Database example — placeholder

Not built yet. This directory reserves the path for the Oracle example.

- **Tracked under:** #44 (`[tracker] v1.1 — More SQL engines`)

No `@db-x/oracle-library` exists yet. Once it lands, this will mirror
[`examples/postgres/`](../postgres/) — a `dbx.tsx` schema + `package.json` +
`.env.example` — targeting Oracle.
