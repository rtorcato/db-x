# MongoDB example — placeholder

Not built yet. This directory reserves the path for the MongoDB example.

- **Exploration:** #42 (`[explore] Document stores: MongoDB + CouchDB`)

MongoDB is a document store, not a relational engine, so it won't mirror the
DDL-based [`examples/postgres/`](../postgres/) directly — the library model is
still being explored (see #42). This path is reserved for when it lands.
