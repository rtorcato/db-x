# DuckDB example — placeholder

Not built yet. This directory reserves the path for the DuckDB example.

- **Example issue:** #36
- **Blocked by:** #30 (`@db-x/duckdb-library`)

Once the library lands, this will mirror [`examples/postgres/`](../postgres/) —
a `dbx.tsx` schema + `package.json` + `.env.example` — targeting DuckDB.
