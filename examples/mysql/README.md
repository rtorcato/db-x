# MySQL example — placeholder

Not built yet. This directory reserves the path for the MySQL example.

- **Example issue:** #13
- **Blocked by:** #12 (`@db-x/mysql-library`)

Once the library lands, this will mirror [`examples/postgres/`](../postgres/) —
a `dbx.tsx` schema + `package.json` + `.env.example` — targeting MySQL.
