# SQL Server example — placeholder

Not built yet. This directory reserves the path for the SQL Server example.

- **Example issue:** #35
- **Blocked by:** #29 (`@db-x/sqlserver-library`)

Once the library lands, this will mirror [`examples/postgres/`](../postgres/) —
a `dbx.tsx` schema + `package.json` + `.env.example` — targeting SQL Server.
